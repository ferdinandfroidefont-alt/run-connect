// Google Maps API global type declarations
// The @types/google.maps package uses a dotted name which TypeScript
// doesn't auto-resolve, so we explicitly reference the type definitions.

/// <reference path="../node_modules/@types/google.maps/index.d.ts" />
