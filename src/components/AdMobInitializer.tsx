/**
 * AdMob initializer - currently a no-op (native SDK removed, placeholder IDs).
 * Phase 2 entry point: re-add the native SDK here and initialize Android/iOS app IDs.
 */
export const AdMobInitializer = () => null;
