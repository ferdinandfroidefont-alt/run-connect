/**
 * Mode diagnostic accueil / Mapbox : carte minimale + shell simple (header + zone carte).
 * Désactiver (`false`) pour retrouver l’écran d’accueil RunConnect complet.
 */
export const MAPBOX_MINIMAL_MODE = true;
