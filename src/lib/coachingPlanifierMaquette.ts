import type { CSSProperties } from "react";

/** Tokens visuels calés sur `PlanifierSemainePage` dans RunConnect (6).jsx */
export const PLANIFIER_MAQUETTE_ACTION_BLUE = "#007AFF";
export const PLANIFIER_MAQUETTE_GROUPED_BG = "#F2F2F7";

/** Exactement comme `style.fontFamily` de la maquette */
export const planifierMaquetteFontStackStyle: CSSProperties = {
  fontFamily: `-apple-system, BlinkMacSystemFont, "SF Pro Display", system-ui, sans-serif`,
  WebkitFontSmoothing: "antialiased",
};